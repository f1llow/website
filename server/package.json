{
  "name": "server",
  "version": "1.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "prisma generate --schema ../prisma/schema.prisma && node index.js",
    "db:deploy": "prisma migrate deploy --schema ../prisma/schema.prisma"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "prisma": "^6.8.2"
  },
  "description": "API server for SpetsVideoTekhnika"
}
